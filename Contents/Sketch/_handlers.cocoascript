@import 'userflows.js'

// Generate a flow
var generateFlow = function(context) {
	parseContext(context)
	askForFlowDetails()
}

var editSettings = function(context) {
	parseContext(context)
	showSettingsDialog()
}
